package leetcode.mj.houzz;

/**
 * 1. ABC，问简历，一个array，最多有是三个distinct的number，也可以只有一个，也可以只有两个，给出来的num1, num2, num3。要求one pass， constant space， leetcode.sort this leetcode.array。其实就是rainbow leetcode.sort。
 * 很快就做掉了，在codepad上写，当场跑代码，bug free 过了后，问了一个OOD的问题，时间不够，答的有些仓促，后来吃饭时想补充一下，又找不到机会。
 * 2. longest increasing subsequence的变种
 * 3. lc 174原题
 * https://instant.1point3acres.com/thread/415835
 *
 */
public class LeetcodeSummaryClass {

    // same as leetcode 75 Sort Colors

    /*
    What Happens When You Type in a URL
    1. You enter a URL into a web browser
    2. The browser looks up the IP address for the domain name via DNS
    3. The browser sends a HTTP request to the server
    4. The server sends back a HTTP response
    5. The browser begins rendering the HTML
    6. The browser sends requests for additional objects embedded in HTML (images, css, JavaScript) and repeats steps 3-5.
    7. Once the page is loaded, the browser sends further async requests as needed.
     */
}
